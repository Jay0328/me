import React from 'react';
//  import PropTypes from 'prop-types';

const Header = () => {
  return (
    <header>
    </header>
  );
};

Header.propTypes = {
};

Header.defaultProps = {
};

export default Header;
