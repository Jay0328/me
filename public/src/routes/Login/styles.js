import { navHeight } from 'Theme/size';

export default {
  form: {
    display: 'flex',
    flexDirection: 'column',
    justifyContent: 'center',
    alignItems: 'center',
    height: '100vh',
    paddingTop: navHeight
  }
};
